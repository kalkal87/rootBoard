# Examples

Runtime examples used to understand and demonstrate the skill without
requiring the separate evaluation repository.

- `failure-cases/` shows the user-facing response for each named runtime
  condition.
- `synthesizer/` provides strong and deliberately uncertain worked synthesis
  shapes used by the synthesizer instructions and translation contract.

Renderer fixtures and automated tests are development material kept outside
the distributed package in the source repository.
